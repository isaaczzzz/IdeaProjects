server:
  port: 80

spring:
  datasource:
    druid:
      driver-class-name: com.mysql.cj.jdbc.Driver
      url: jdbc:mysql://localhost:3306/mybatis?serverTimezone=UTC
      username: root
      password: isaacZ1231

# 表名前缀
mybatis-plus:
  global-config:
    db-config:
      table-prefix: tb_
      id-type: auto #id生成策略
